#ifndef BATCHRNG_KERNEL_H
#define BATCHRNG_KERNEL_H
//For the reduce sum function
#include "naive_kernel.hu"

__global__ void batchrng_kernel (float *const results,
				 float *const angleVec,
				 float *const distVec,
				 const unsigned int numSims);

#endif
